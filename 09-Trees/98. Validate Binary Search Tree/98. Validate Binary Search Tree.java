            while(curr != null) {                st.push(curr);                curr = curr.left;            }            curr = st.pop();            if(prev != null && prev.val >= curr.val) {                return false;            }            prev = curr;            curr = curr.right;        }        return true;    }}